{
  "name": "focus-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:server": "node server/index.js",
    "dev:full": "concurrently -n app,api -c cyan,green \"npm run dev\" \"npm run dev:server\"",
    "build": "vite build",
    "preview": "vite preview",
    "postinstall": "cd server && npm install"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "concurrently": "^9.0.0",
    "svelte": "^5.55.5",
    "vite": "^8.0.12",
    "vite-plugin-pwa": "^1.3.0"
  }
}
